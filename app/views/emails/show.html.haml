- provide(:title, 'メール・通知設定')

%header
  %h1 設定

%hr

= render 'settings/nav'

%section
  = form_with url: email_path, method: :patch, local: true do |form|

    = render 'shared/error_messages', errors: @errors

    .form-group
      = form.label :email, 'メールアドレス'
      = form.email_field :email, value: @auth0_user.email, class: 'form-control'

    .form-group
      .custom-control.custom-checkbox
        = form.check_box :subscribing, checked: @auth0_user.subscribing?, class: 'custom-control-input'
        = form.label :subscribing, class: 'custom-control-label' do
          LiveLog からのお知らせメールを受け取る
          = icon 'fas', 'question-circle', 'data-toggle': 'tooltip', 'data-placement': 'right', title: '演奏した曲が今日のピックアップに選ばれた時などにお知らせします'

    = form.submit '更新する', class: 'btn btn-primary'

