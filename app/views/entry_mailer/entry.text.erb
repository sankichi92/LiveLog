申請者: <%= "#{@entry.applicant.name} (#{@entry.applicant.joined})" %>

ライブ: <%= @entry.live.title %>

曲名 / アーティスト名: <%= @entry.title %><%= @entry.song.original? ? ' （オリジナル曲）' : ''%>

メンバー:
<% @entry.playings.reverse.each do |playing| %>
    <%= "#{playing.inst}. #{playing.member.full_name} (#{playing.member.joined_year})" %>
<% end %>

リハーサル希望時間:
<%= @entry.preferred_rehearsal_time %>

演奏希望時間:
<%= @entry.preferred_performance_time %>

備考:
<%= @entry.notes %>
