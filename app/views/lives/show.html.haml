- provide(:title, @live.title)
- provide(:description, "#{l(@live.date)} @#{@live.place} #{@songs.map.with_index { |s, i| "#{i + 1}.#{s.name}" }.join(' ')}")

%article
  %header.card.mb-3
    .card-body.lead
      .card-subtitle.text-muted= l @live.date
      %h1.card-title= @live.name
      %p.card-text.text-muted= @live.at_place
      - if current_user && @live.album_url?
        = link_to icon('fas', 'images', 'アルバム'), @live.album_url, class: 'card-link btn btn-primary', target: '_blank', rel: 'noopener'

  .song-list
    = render @songs
