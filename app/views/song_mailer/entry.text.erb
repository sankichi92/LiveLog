申請者: <%= "#{@applicant.name} (#{@applicant.joined})" %>

ライブ: <%= @song.live_title %>

曲名 / アーティスト名: <%= @song.title %>

メンバー:
<% @song.playings.reverse.each do |playing| %>
    <%= "#{playing.inst}. #{playing.name} (#{playing.joined})" %>
<% end %>

リハーサル希望時間:
<%= @song.rehearsal_time %>

演奏希望時間
<%= @song.play_time %>

備考:
<%= @song.notes %>
