%header.content-header
  .container-fluid
    .row.mb-3
      .col-sm-6
        %h1
          = Live.model_name.human
          新規作成

      .col-sm-6
        %ol.breadcrumb.float-sm-right
          %li.breadcrumb-item= link_to 'Home', admin_root_path
          %li.breadcrumb-item= link_to Live.model_name.human, admin_lives_path
          %li.breadcrumb-item.active 新規作成

%section.content
  .container-fluid
    %section.card
      = form_with model: @live, url: admin_lives_path, local: true do |form|
        .card-body
          = render 'shared/error_messages', errors: @live.errors

          .form-group
            = form.label :date
            = form.date_field :date, required: true, class: 'form-control'

          .form-group
            = form.label :name
            = form.text_field :name, required: true, class: 'form-control'

          .form-group
            = form.label :place
            = form.text_field :place, class: 'form-control'

          .form-group
            = form.label :album_url
            = form.url_field :album_url, class: 'form-control', placeholder: 'https://goo.gl/photos/o94hbpFHQtcjzjwj6'
            %small.form-text.text-muted 共同編集・コメント可能な <a href="https://photos.google.com" target="_blank">Google フォト</a> の共有アルバム推奨です

        .card-footer
          = form.submit class: 'btn btn-primary'
